[project]
name = "utils-py"
version = "0.1.0"
description = "A bunch of utility scripts that I use from time to time."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyperclip>=1.11.0",
    "requests>=2.32.5",
    "wordfreq>=3.1.1",
]
